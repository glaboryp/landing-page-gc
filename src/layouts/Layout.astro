---
import { ViewTransitions } from 'astro:transitions'
import Navbar from "../components/Navbar.astro"

interface Props {
	title: string;
}

const { title } = Astro.props
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Página que auna todas las herramientas del proyecto Global Constallation" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-slate-100 dark:bg-[#0a101f] dark:text-slate-100">
		<Navbar />
		<slot />
	</body>
</html>

